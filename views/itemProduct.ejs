<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/css/global.css" />
    <link rel="stylesheet" href="/css/itemProduct.css" />
    <title><%= title %></title>
  </head>
  <body>
    <%- include('partials/navbar') %>

    <div class="container my-5">
      <div class="card mb item-product-card">
        <div class="row g-0">
          <div class="col-md">
            <img
              src="<%= item.image_url %>"
              class="img-fluid rounded-start"
              alt="<%= item.name %>"
            />
          </div>
          <div class="col-md">
            <div class="card-body">
              <!-- linting error for background-color but renders fine -->
              <p
                class="card-text category-tag"
                style="background-color: <%= item.category_tag %>"
              >
                <%= item.category_name || 'Category Unavailable' %>
              </p>
              <h5 class="card-title"><%= item.name %></h5>

              <p class="card-text">
                <%= item.brand || 'No brand available.' %>
              </p>
              <p class="card-text">
                €<%= item.price || 'No price available.' %>
              </p>
              <p class="card-text">
                <small class="text-body-secondary"
                  >Available Sizes: <%= item.size ? item.size : 'unknown'
                  %></small
                >
              </p>
              <div class="btn-container search-product-button-container">
                <a
                  href="https://stockx.com/search?s=<%= encodeURIComponent(item.name) %>"
                  class="btn stock-x-button"
                  target="_blank"
                  >Search StockX</a
                >
                <a
                  href="https://goat.com/search?query=<%= encodeURIComponent(item.name) %>"
                  class="btn goat-button"
                  target="_blank"
                  >Search GOAT</a
                >
                <a
                  href="https://flightclub.com/catalogsearch/result?query=<%= encodeURIComponent(item.name) %>"
                  class="btn flight-club-button"
                  target="_blank"
                  >Search Flight Club</a
                >
                <a
                  href="https://int.stadiumgoods.com/en-ie/shopping?pageindex=1&query=<%= encodeURIComponent(item.name) %>"
                  class="btn stadium-goods-button"
                  target="_blank"
                  >Search Stadium Goods</a
                >
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
